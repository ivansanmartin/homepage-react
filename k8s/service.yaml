apiVersion: v1
kind: Service
metadata:
  name: homepage-service
  namespace: #--
spec:
  type: ClusterIP
  selector:
    app: nginx-pod-label
  ports:
    - protocol: TCP
      port: #--
      targetPort: #--