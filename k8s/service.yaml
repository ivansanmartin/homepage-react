apiVersion: v1
kind: Service
metadata:
  name: homepage-service
  namespace: ivansanmartin
spec:
  type: ClusterIP
  selector:
    app: homepage-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80